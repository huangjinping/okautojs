var appName="CallCenter-Ghana";

// app.launchApp("CallCenter-Ghana");
click(appName);
sleep(5000);
if(text("仅在使用中允许").findOnce()){
    text("仅在使用中允许").findOnce().click();
}
if(text("仅在使用中允许").findOnce()){
    text("仅在使用中允许").findOnce().click();
}
if(text("仅在使用中允许").findOnce()){
    text("仅在使用中允许").findOnce().click();
}
if(text("仅在使用中允许").findOnce()){
    text("仅在使用中允许").findOnce().click();
}
if(text("仅在使用中允许").findOnce()){
    text("仅在使用中允许").findOnce().click();
}
sleep(1000);
text("Sign In",1).click();

